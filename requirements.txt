# Modern requirements for the legacy analytics tool.
# These versions are compatible with Python 3.8+ and should install cleanly
# on modern systems while preserving the original analytics semantics.

# Core scientific libraries
pandas>=1.3.0,<3.0.0
numpy>=1.21.0,<3.0.0

# Testing framework (optional but recommended)
pytest>=7.0.0,<9.0.0
